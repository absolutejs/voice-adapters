{
	"name": "@absolutejs/voice-google-speech",
	"version": "0.0.1-beta.2",
	"description": "Google Cloud Speech-to-Text adapter for @absolutejs/voice — buffered-batch (REST) + real-time streaming (gRPC-Web over HTTP/2, no @grpc/grpc-js dep)",
	"repository": {
		"type": "git",
		"url": "https://github.com/absolutejs/voice-adapters.git",
		"directory": "google-speech"
	},
	"files": ["dist", "README.md"],
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"import": "./dist/index.js",
			"types": "./dist/index.d.ts"
		}
	},
	"license": "CC BY-NC 4.0",
	"author": "Alex Kahn",
	"scripts": {
		"build": "rm -rf dist && bun build ./src/index.ts --outdir dist --target bun --external @absolutejs/voice && tsc --emitDeclarationOnly --project tsconfig.json",
		"format": "prettier --write \"./**/*.{js,ts,json,md}\"",
		"release": "bun run format && bun run build && bun publish --access public",
		"test": "bun test",
		"typecheck": "bun run tsc --noEmit"
	},
	"dependencies": {
		"@absolutejs/voice": "0.0.22-beta.471"
	},
	"devDependencies": {
		"@types/bun": "1.3.9",
		"typescript": "^5.9.3"
	}
}
